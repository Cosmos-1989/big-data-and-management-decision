"""Regression tests for bdm_decision.models — pending implementation."""

import pytest


@pytest.mark.skip(reason="待补充：churn_model / demand_forecast / delay_risk_model 的回归测试")
def test_prediction_models_regression() -> None:
    ...
